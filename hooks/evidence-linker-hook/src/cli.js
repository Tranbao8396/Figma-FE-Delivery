const fs=require("fs"),path=require("path"),{buildEvidenceLinks}=require("./hook");const a={};for(let i=2;i<process.argv.length;i+=2)a[process.argv[i].replace(/^--/,"")]=process.argv[i+1];if(!a.input||!a.out)throw new Error("Usage: --input <evidence-input.json> --out <evidence-links.json>");const out=path.resolve(a.out),previous=fs.existsSync(out)?JSON.parse(fs.readFileSync(out,"utf8")):null,result=buildEvidenceLinks(JSON.parse(fs.readFileSync(path.resolve(a.input),"utf8")),{previousContext:previous});if(!result.cacheHit){fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,JSON.stringify(result.context));}console.log(JSON.stringify({cacheHit:result.cacheHit,outPath:out,coverage:result.context.coverage,verdict:result.context.verdict}));
