const fs=require("fs"),path=require("path"),{buildDesignIndex}=require("./hook");const a={};for(let i=2;i<process.argv.length;i+=2)a[process.argv[i].replace(/^--/,"")]=process.argv[i+1];if(!a.raw||!a.out)throw new Error("Usage: --raw <raw.json> --out <design-index.json>");const out=path.resolve(a.out),previous=fs.existsSync(out)?JSON.parse(fs.readFileSync(out,"utf8")):null,result=buildDesignIndex(JSON.parse(fs.readFileSync(path.resolve(a.raw),"utf8")),{rawArtifact:path.resolve(a.raw),previousContext:previous});if(!result.cacheHit){fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,JSON.stringify(result.context));}console.log(JSON.stringify({cacheHit:result.cacheHit,outPath:out,...result.context.diagnostics}));
